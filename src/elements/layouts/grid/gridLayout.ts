/* import { Parsed } from "@src/positional/parsing/parsed";
import { Rectangle } from "@src/positional/rectangle";
import { Layoutable } from "@src/windows/layoutable";
import { WidgetMap } from "@src/windows/widgets/widgetMap";
import { GridCellParams } from "./grid";
import { GridPosition } from "./gridPosition";


export function gridLayout(elements: Layoutable<Parsed<GridPosition>>, parentArea: Rectangle, widgetMap: WidgetMap): void
{

}


function parseGridStack(cells: GridCellParams)
{
	const cellsCount = cells.count;
	const stack = createStack<ParsedScale>(cellsCount, cells.gap || zeroScale);

	// First pass: parse all values to numbers.
	for (let i = 0; i < cellsCount; i++)
	{

	}
}
 */
